{{- if .Values.files }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiv.fullname" . }}-files
  labels:
{{ include "aiv.labels" . | indent 4 }}

data:
{{- range $path, $content := .Values.files }}
  {{ $path }}: |-
{{ $content | indent 4 }}
{{- end }}
{{- end }}
