apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiv.fullname" . }}
  labels:
    {{- include "aiv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiv.selectorLabels" . | nindent 4 }}
